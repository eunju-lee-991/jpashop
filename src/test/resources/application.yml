spring: # springboot에서 jdbc:h2:mem:test 안해줘도 자동으로 메모리로 동작
#  datasource:
#    url: jdbc:h2:mem:test # 테스트에서 메모리 디비로 동작
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create // springboot에서는 create-drop이 기본
#    properties:
#      hibernate:
##        show_sql: true - sysout 통해서 찍음
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug  # log로 찍음
    org.hibernate.type: trace